package com.vendify.accounts.exceptions;

public class SecurityException extends GenericException {
    public SecurityException(String message, String description) {
        super(message, description);
    }
}
